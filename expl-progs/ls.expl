int main()
{
decl
    int buffer;
    int tmp, fd, tmp1;
enddecl

begin
    fd = exposcall("Open", "root");
    tmp = 0;
    while (tmp != -2) do
        tmp = exposcall("Read", fd, buffer);
        tmp1 = exposcall("Write", -2, buffer);
    endwhile;
    return 0;
end
}
